"use client";

import { ComingSoon } from "@/components/layout/ComingSoon";

export default function ResolverPage() {
  return <ComingSoon title="Resolver" description="Configure VPC DNS resolvers, endpoints and forwarding rules." />;
}
